# Basic dependencies for klik_pos
# Note: frappe and erpnext are installed via bench, not pip
# These are the actual dependencies needed for testing

# Development dependencies
pytest>=7.0.0
pytest-cov>=4.0.0
black>=24.0.0
ruff>=0.1.0
isort>=5.12.0
pre-commit>=3.0.0

# Testing dependencies
requests>=2.28.0
python-dateutil>=2.8.0
pytest-mock>=3.10.0

# Frappe/ERPNext related (if needed for testing)
# These are usually installed via bench install-app
# frappe-bench>=1.0.0
